- name: variable preference
  hosts: local
  connection: local
  vars:
   NAME: "Ansible from play"
  vars_files:
  - Course.yaml
  vars_prompt:
  - name: NAME
    prompt: please enter name
    private: false
  tasks:
  - name: print name
    vars:
      NAME: "Ansible from tast"
    ansible.builtin.debug:
      mas: "Hi {{ NAME }}"    
  


  
